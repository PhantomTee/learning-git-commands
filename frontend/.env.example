# Required. Without it the Convex client logs a fatal parse error on every page
# load — the app still renders, but the activity feed and notifications break.
# Get this from `npx convex dev`.
NEXT_PUBLIC_CONVEX_URL=

# Optional. Used as the metadataBase for OpenGraph card URLs.
NEXT_PUBLIC_SITE_URL=
